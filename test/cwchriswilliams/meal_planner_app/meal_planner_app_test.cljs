(ns cwchriswilliams.meal-planner-app.meal-planner-app-test
  (:require [cljs.test :refer [deftest testing is]]))

(deftest sucessful-test
  (testing "That test runner is running correctly"
    (is (= 1 1))))